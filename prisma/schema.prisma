datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
    ssl      = "require"
}

generator client {
    provider = "prisma-client-js"
}

model Product {
    id          Int    @id @default(autoincrement())
    slug        String
    name        String
    price       Float
    description String
    imageUrl    String
}
